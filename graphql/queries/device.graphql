query ListDevices(
  $pagination: OffsetPaginationInput
  $filters: ManagementDeviceFilter
  $order: ManagementDeviceOrder
) {
  devices(pagination: $pagination, filters: $filters, order: $order) {
    ...ListDevice
  }
}

query GetDevice($id: ID!) {
  device(id: $id) {
    ...DetailDevice
  }
}
